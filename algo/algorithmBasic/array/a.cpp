#include <bits/stdc++.h>
using namespace std;
#define rep(i, n) for (int i = 0; i < (n); i++)
using P = pair<int, int>;
using ll = long long;

int main() {
  vector<int> a = {3, 1, 4, 1, 5, 9, 2, 6, 5, 3};
  int k;
  cin >> k;

  cout << a[k] << endl;
}