#include <bits/stdc++.h>
using namespace std;
#define rep(i, n) for (int i = 0; i < (n); i++)
using P = pair<int, int>;
using ll = long long;

int main() {
  int n, k;
  cin >> n >> k;
  rep(i, n) {
    int ok = -1;
    int ng = 2 * n * n;
    while (ng - ok > 1) {
      int mid = (ok + ng) / 2;
    }
  }
}