#include <bits/stdc++.h>
using namespace std;
#define rep(i, n) for (int i = 0; i < (n); i++)
using P = pair<int, int>;
using ll = long long;

int main() {
  int s;
  cin >> s;

  if (s >= 200 && s <= 299) {
    cout << "Success" << endl;
    return 0;
  } else {
    cout << "Failure" << endl;
    return 0;
  }
}