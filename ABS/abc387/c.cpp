#include <bits/stdc++.h>
using namespace std;
#define rep(i, n) for (int i = 0; i < (n); i++)
using P = pair<int, int>;
using ll = long long;

ll func(string l, string r) {
  ll ans = 0;
  if (l == r) {
    return ans;
  }
}

int main() {
  string l, r;
  cin >> l >> r;
}