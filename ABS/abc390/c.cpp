#include <bits/stdc++.h>
using namespace std;
#define rep(i, n) for (int i = 0; i < (n); i++)
using P = pair<int, int>;
using ll = long long;

int main() {
  int h, w;
  cin >> h >> w;
  vector<string> s(h);
  rep(i, h) cin >> s[i];

  int a = 0, b = 0, c = 0, d = 0;
  rep(i, h) {
    rep(j, w) {
      if (s[i][j] == '#') {

        continue;
      }
    }
  }
}

// cout << "Yes" << endl;
